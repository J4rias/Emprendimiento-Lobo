import { z } from 'zod';

export const CreateSaleSchema = z.object({}).passthrough();
export const UpdateSaleSchema = z.object({}).passthrough();
export const ValidateCreditPinSchema = z.object({}).passthrough();
export const CancelSaleSchema = z.object({}).passthrough();
export const AddPaymentSchema = z.object({}).passthrough();
